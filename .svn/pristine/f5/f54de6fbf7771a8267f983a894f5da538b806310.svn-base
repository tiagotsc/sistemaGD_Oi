let PERFIL_ADM = '1'; // Administrador
let PERFIL_SOL = '6'; // Solicitante
let PERFIL_EQU = '9'; // Equipe

// Demanda - Tela pesquisa
let btDemEditar = [];
btDemEditar[PERFIL_ADM] = ['1','2','3','4']; // Administrador
btDemEditar[PERFIL_EQU] = ['1','2','3','4']; // Equipe
btDemEditar[PERFIL_SOL] = ['1']; // Solicitante

let btDemVisualizar = [PERFIL_ADM, PERFIL_SOL, PERFIL_EQU]; // Botão visualizar

let btDemDelegar = [PERFIL_ADM]; // Botão delegar

let btDemDilatar = []; // Botão dilatar
btDemDilatar[PERFIL_ADM] = ['2','3','4']; // Administrador
btDemDilatar[PERFIL_EQU] = ['2','3','4']; // Equipe
btDemDilatar[PERFIL_SOL] = []; // Solicitante

let btDemTratar = []; // Botão tratar
btDemTratar[PERFIL_ADM] = ['2','3','4']; // Administrador
btDemTratar[PERFIL_EQU] = ['2','3','4']; // Equipe
btDemTratar[PERFIL_SOL] = []; // Solicitante

let btDemCancelar = []; // Botão cancelar
btDemCancelar[PERFIL_ADM] = ['1','2','3']; // Administrador
btDemCancelar[PERFIL_EQU] = ['1','2','3']; // Equipe
btDemCancelar[PERFIL_SOL] = ['1']; // Solicitante

let btDemReabrir = [PERFIL_ADM, PERFIL_EQU]; // Botão reabrir

// Demanda - Tela de edição 
let btDemRemoverAnexo = ['1','2'] // (Aberto, Em tratamento)

// Fator Gerador - Tela pesquisa
let btFtEditar = [PERFIL_ADM];